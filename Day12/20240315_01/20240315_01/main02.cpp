#include <iostream>
#include <vector>
using namespace std;

void main()
{
	// Vector => 사이즈가 변할 수 있는 '배열'
	// 일반적인 배열 : 메모리가 연속적이다.
	//		단점 => push_back할 때 너무 무겁다.
	//			 => 중간 추가/삭제가 어렵다
	//		장점 => 접근 속도가 빠르다
	//				ex) at() 시간 복잡도가 O(1), Linked List는 O(N)임.
	// new, delete => 쓸 수 있는 함수 중에 제일 무거운 함수
	// 사칙연산보다 훨씬 느리다.

	// 뭔가 수업이 있었는데 딴짓해서 놓침. 알아서 복붙.
}