#include <iostream>
using namespace std;

void main() {
    // 사칙연산
    // 곱하기 빼기 더하기 나누기 + 나머짓값

    int num1 = 10;
    int num2 = 5;
    int resultSum = num1 + num2;
    int resultSubtract = num1 - num2;
    int resultMultiply = num1 * num2;
    int resultDivide = num1 / num2;
    int resultRemain = num1 % num2;

    printf("resultSum = %d\n", resultSum);
    printf("resultSubtract = %d\n", resultSubtract);
    printf("resultMultiply = %d\n", resultMultiply);
    printf("resultDivide = %d\n", resultDivide);
    printf("resultRemain = %d\n", resultRemain);

    // 변수명 규칙 (무조건 암기) (거의 비슷)
    // 영어로 시작
    // 한글 사용 X
    // 소문자로 시작
    // 단어 두 개를 조합할 일이 생기면 (스페이스 쓸 일이 생기면) 대문자로 구분

    //int myNumber;
    //int MyNumber;
    //int my_number;
}